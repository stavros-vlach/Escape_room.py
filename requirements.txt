# Python dependencies for Escape Room - LLM Version
# For template version (puzzle.py), no dependencies needed!

# Core LLM libraries
transformers>=4.30.0
torch>=2.0.0
accelerate>=0.20.0

# Optional: For faster CPU inference
# sentencepiece>=0.1.99

# Note: First run will download ~15GB Mistral-7B model
# Model will be cached in ~/.cache/huggingface/
